# Анализ фронтенд кодовой базы: otis-valen

## 📁 Структура проекта
- Многостраничный статический сайт: `index.html`, `about.html`, `work.html`, `project.html`, `contact.html`
- `js/` — модули поведения: `menu.js`, `hero.js`, `work.js`, `project.js`, `about.js`, `transition.js`, `lenis-scroll.js`, `footer.js`, `services.js`, `contact.js`
- `css/` — стили
- `public/` — статика

Дерево (до 3 уровней):
- `js/` (около 10 модулей поведения)
- `css/`
- корень: HTML файлы страниц

Организация: layer-based — статический HTML + модульные JS для анимаций и интерактива.

## 🛠 Технологический стек
- **Тулчейн**: Vite (dev/preview), но рендер статический
- **Язык**: Vanilla JavaScript
- **Анимации**: GSAP
- **Скролл**: Lenis (через модуль `lenis-scroll.js`)

## 🏗 Архитектура
- JS-модули инициализируются на `DOMContentLoaded`, находят элементы и навешивают логику/анимации.
- Меню-оверлей реализован в `menu.js` с блокировкой скролла и анимацией элементов.

Пример — логика меню (сокр.):
```js
document.addEventListener('DOMContentLoaded', () => {
  const btn = document.querySelector('.menu-toggle-btn');
  const overlay = document.querySelector('.nav-overlay');
  let isOpen = false;
  btn.addEventListener('click', () => {
    gsap.to(overlay, { opacity: isOpen ? 0 : 1, duration: 0.3 });
    isOpen = !isOpen;
  });
});
```

## 🎨 UI/UX и стилизация
- Кастомный дизайн, плавные анимации, оверлей-меню.
- Доступность: важно добавить aria-атрибуты к кнопкам/меню, фокус-ловушку для оверлея.

## ✅ Качество кода
- Разделение на модули, читаемые функции. Линтер не настроен.
- Тесты: отсутствуют.

## 🔧 Ключевые модули
- `menu.js` — управление оверлеем навигации и блокировкой скролла
- `transition.js` — переходы между секциями/страницами
- `lenis-scroll.js` — плавная прокрутка

## 📋 Выводы и рекомендации
- Сильные стороны: модульность, плавные анимации, понятная архитектура.
- Улучшения: добавить ESLint/Prettier, базовые тесты для критичной логики, aria/keyboard support.
- Уровень: junior→middle.