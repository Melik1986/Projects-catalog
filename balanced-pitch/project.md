# Анализ фронтенд кодовой базы: balanced-pitch

## 📁 Структура проекта
- `src/pages/` — страницы: `home`, `about`, `updates`, `solutions`, `contact`
- `src/components/` — `Menu`, `Footer`, `ParallaxImage`, `MusicPlayer`, `Transition`
- `public/` — статические ресурсы

Дерево (до 3 уровней):
- `src/`
  - `pages/` (`home/`, `about/`, `updates/`, `solutions/`, `contact/`)
  - `components/` (`Menu/`, `Footer/`, `ParallaxImage/`, `MusicPlayer/`, `Transition/`)

Организация: layer-based — отдельные страницы + переиспользуемые UI/эффекты.

## 🛠 Технологический стек
- **Фреймворк**: React 18.3.1, Vite 5.4
- **Язык**: JavaScript
- **Роутинг**: `react-router-dom` 7.x
- **Анимации**: Framer Motion, GSAP, Lenis (или аналогичные техники параллакса)

## 🏗 Архитектура
- Компоненты: `Menu` управляет состоянием открытия/темой, `ParallaxImage` — визуальные эффекты.
- Роутинг: `<AnimatePresence>` для анимированных переходов между страницами.
- SEO/UX: динамический `document.title`, автоскролл к началу страницы при навигации.

Пример — App со сменой заголовков и анимированными роутами:
```jsx
useEffect(() => {
  document.title = pageTitles[location.pathname] || 'Balanced Pitch';
  setTimeout(() => window.scrollTo(0, 0), 750);
}, [location.pathname]);
<AnimatePresence mode="wait" initial={false}>
  <Routes location={location} key={location.pathname}>
    <Route index element={<Home />} />
    <Route path="/updates" element={<Updates />} />
  </Routes>
</AnimatePresence>
```

## 🎨 UI/UX и стилизация
- Кастомный UI, параллакс и музыкальный плеер, адаптивная верстка.
- Доступность: стоит добавить aria-атрибуты для меню, контролов плеера, фокус-ловушки.

## ✅ Качество кода
- ESLint конфиг присутствует, единый стиль, структура понятна.
- Тесты: не обнаружены.

## 🔧 Ключевые компоненты
- `Menu` — глобальная навигация и тема (isDark)
- `ParallaxImage` — скролл-эффекты
- `MusicPlayer` — управление аудио

## 📋 Выводы и рекомендации
- Сильные стороны: чистая структура, UX-детали (скролл и заголовки), анимированные переходы.
- Улучшения: базовые тесты, централизованное логирование ошибок, TS на публичные API компонентов.
- Уровень: middle.