# 🚀 Развертывание проекта на Vercel

## Структура проекта
Этот проект состоит из двух частей:
- **Клиент** (React + Vite) - развертывается на Vercel
- **Сервер** (Express.js) - развертывается на отдельной платформе

## 📦 Отдельный репозиторий для сервера

Создан отдельный репозиторий `threejs-ai-server` для упрощения развертывания:
- Папка: `project_threejs_ai-3D/threejs-ai-server/`
- Содержит только серверный код
- Готов для развертывания на Render/Railway

## 📋 Пошаговая инструкция

### 1. Развертывание клиента на Vercel

#### Вариант A: Через Vercel Dashboard
1. Зайдите на [vercel.com](https://vercel.com)
2. Нажмите "New Project"
3. Импортируйте ваш GitHub репозиторий `Melik1986/Projects-catalog`
4. **Важно**: Установите Root Directory как `project_threejs_ai-3D/client`
5. Настройки сборки:
   - **Build Command**: `npm run build`
   - **Output Directory**: `dist`
   - **Install Command**: `npm install`
6. Нажмите "Deploy"

#### Вариант B: Через Vercel CLI
```bash
# Установите Vercel CLI
npm i -g vercel

# Перейдите в папку клиента
cd project_threejs_ai-3D/client

# Разверните
vercel

# Следуйте инструкциям CLI
```

#### Вариант C: Использовать vercel.json (уже создан)
Файл `vercel.json` уже создан в корне проекта и настроен для автоматического развертывания клиента.

### 2. Развертывание сервера

#### Вариант A: Использовать отдельный репозиторий (Рекомендуется)

**Создайте новый GitHub репозиторий:**
1. Создайте новый репозиторий на GitHub: `threejs-ai-server`
2. Скопируйте содержимое папки `project_threejs_ai-3D/threejs-ai-server/` в новый репозиторий
3. Загрузите код на GitHub

**Развертывание на Render:**
1. Зайдите на [render.com](https://render.com)
2. Создайте новый "Web Service"
3. Подключите репозиторий `threejs-ai-server`
4. Настройки:
   - **Root Directory**: (оставить пустым)
   - **Build Command**: `npm install`
   - **Start Command**: `npm start`
5. Добавьте переменные окружения:
   ```
   OPENAI_API_KEY=your_openai_api_key
   ```

#### Вариант B: Использовать существующий репозиторий

**На Railway (Рекомендуется)**
1. Зайдите на [railway.app](https://railway.app)
2. Создайте новый проект
3. Подключите GitHub репозиторий `Projects-catalog`
4. Выберите папку `project_threejs_ai-3D/threejs-ai-server`
5. Добавьте переменные окружения:
   ```
   OPENAI_API_KEY=your_openai_api_key
   ```
6. Railway автоматически определит Node.js и развернет

**На Render (Текущий способ)**
1. Зайдите в настройки вашего существующего сервиса на Render
2. Измените настройки:
   - **Root Directory**: `project_threejs_ai-3D/threejs-ai-server`
   - **Build Command**: `npm install`
   - **Start Command**: `npm start`
3. Добавьте переменные окружения:
   ```
   OPENAI_API_KEY=your_openai_api_key_here
   ```
4. Сохраните изменения и перезапустите развертывание

### 3. Обновление конфигурации

После развертывания сервера обновите `client/src/config/config.js`:

```javascript
const config = {
  development: {
    backendUrl: "http://localhost:8080/api/v1/dalle",
  },
  production: {
    backendUrl: "https://threejs-ai-server.onrender.com/api/v1/dalle", // ← Обновлено
  },
};
```

✅ **Сервер развернут**: https://threejs-ai-server.onrender.com

### 4. Пересборка клиента
После обновления конфигурации пересоберите клиент на Vercel.

## 🔧 Альтернативные решения

### Полное развертывание на одной платформе

#### На Railway (Full-stack)
1. Создайте два сервиса в одном проекте:
   - **Frontend**: папка `client`
   - **Backend**: папка `server`
2. Настройте переменные окружения для backend
3. Обновите конфигурацию клиента

#### На Render (Full-stack)
1. Создайте два сервиса:
   - **Static Site**: папка `client`
   - **Web Service**: папка `server`

## 📝 Важные замечания

- **CORS**: Убедитесь, что сервер настроен для работы с вашим доменом Vercel
- **Переменные окружения**: Не забудьте добавить `OPENAI_API_KEY` на сервер
- **Порты**: Vercel автоматически назначает порты, не указывайте их явно

## 🎯 Рекомендуемый подход

1. **Клиент на Vercel** (бесплатно, быстро)
2. **Сервер на Railway** (бесплатный план, простой деплой)
3. Обновить конфигурацию клиента
4. Пересобрать на Vercel

Этот подход обеспечивает лучшую производительность и простоту управления.
