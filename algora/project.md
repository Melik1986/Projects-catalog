# Анализ фронтенд кодовой базы: algora

## 📁 Структура проекта
- `src/app/` — Next.js App Router: `layout.js`, `page.js`, глобальные стили, вложенные маршруты (`archive/`).
- `src/components/` — переиспользуемые UI-компоненты: `Navbar`, `Footer`, `Marquee`, `ShuffleText`, `GeometricBackground`, `MusicToggle`, `ProgressBar`.
- `public/` — статические ресурсы и иконки.

Дерево директорий (до 3 уровней):
- `src/`
  - `app/`
    - `archive/`
    - `layout.js`, `page.js`, `globals.css`, `home.css`
  - `components/`
    - `Navbar/`, `Footer/`, `Marquee/`, `ShuffleText/`, `GeometricBackground/`, `MusicToggle/`, `ProgressBar/`

Принципы организации: layer-based + App Router. Вся навигация и композиция страниц — в `app/`, UI в `components/`.

## 🛠 Технологический стек
- **Фреймворк**: Next.js 15.1.5 (App Router), React 18.2.0
- **Сборка**: next build/start, `next lint`
- **Язык**: JavaScript
- **CSS**: глобальные CSS (`globals.css`, `home.css`), CSS Modules (папка присутствует)
- **Состояние**: локальные хуки; плавный скролл `@studio-freight/react-lenis`
- **Анимации**: GSAP (+ ScrollTrigger), `@gsap/react`
- **Иконки**: `react-icons`

Основные зависимости и назначение:
- `gsap`, `@gsap/react`, `gsap/ScrollTrigger` — сложные анимации и привязка к скроллу
- `@studio-freight/react-lenis` — плавный скролл и пиннинг секций
- `next`, `react`, `react-dom` — каркас приложения

## 🏗 Архитектура
- Компонентный подход: контентные секции в `app/page.js`, презентационные блоки в `components/*`.
- Разделение логики: хуки `useEffect`, `useGSAP`; композиция эффектов; минимум глобального состояния.
- Роутинг: App Router (`app/layout.js`, `app/page.js`), вложенные маршруты (`app/archive`).
- Работа с данными: статический/контентный рендеринг, внешние API не используются явно.
- Ошибки/загрузка: базовые паттерны Next (страница `not-found.jsx`).

Пример кода — GSAP + ScrollTrigger, пиннинг карусели:
```jsx
// src/app/page.js (фрагмент)
useGSAP(() => {
  const projects = gsap.utils.toArray('.project');
  ScrollTrigger.create({
    trigger: '.carousel', start: 'top top',
    end: `+=${window.innerHeight * (projects.length - 1)}`,
    pin: true, pinSpacing: true, scrub: 1,
    onUpdate: (self) => {
      const progress = self.progress * (projects.length - 1);
      const current = Math.floor(progress);
      const slideProgress = progress - current;
      // обновление clip-path для слайдов...
    },
  });
});
```

## 🎨 UI/UX и стилизация
- Кастомная визуализация с акцентом на скролл-анимациях и геом. фонах.
- Используются модульные и глобальные стили; отзывчивость — через CSS и брейкпоинты.
- Доступность: стоит дополнительно проверить ARIA и контраст элементов с интенсивными эффектами.

## ✅ Качество кода
- Линтинг: `next lint` в скриптах. Единый стиль кода.
- Типизация: JavaScript, без TS.
- Тесты: отсутствуют явные конфигурации тестов.
- Документация: README присутствует, в коде — минимальные комментарии.

## 🔧 Ключевые компоненты
- `GeometricBackground` — фоновая анимация, реагирующая на скролл
- `Marquee`, `ShuffleText` — декоративные текстовые эффекты
- `Navbar`, `Footer` — каркас навигации

Пример использования Lenis:
```jsx
<ReactLenis root options={{ duration: 1.5, smooth: true }}>
  <div className="app">{children}</div>
</ReactLenis>
```

## 📋 Выводы и рекомендации
- Сильные стороны: зрелые анимации, современный App Router, чистое разделение UI.
- Улучшения: добавить базовые тесты (RTL/E2E), ввести TypeScript для ключевых модулей, стандартизировать логи/обработку ошибок.
- Уровень: middle→upper-middle (сложные анимации GSAP/ScrollTrigger).