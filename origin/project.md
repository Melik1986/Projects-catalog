# Анализ фронтенд кодовой базы: origin

## 📁 Структура проекта
- `src/pages/` — страницы `Home`, `Work`
- `src/components/` — `Transition`, `NavBar`, `HeroGradient`, `Cursor`, `VideoPlayer`, `BackButton`
- `public/` — статика

Дерево (до 3 уровней):
- `src/`
  - `pages/` (`Home/`, `Work/`)
  - `components/` (`Transition/`, `NavBar/`, `HeroGradient/`, `Cursor/`, `VideoPlayer/`, `BackButton/`)

Организация: layer-based — страницы и переиспользуемые компоненты, анимированные переходы.

## 🛠 Технологический стек
- **Фреймворк**: React 18.3.1, Vite 6.0.1
- **Язык**: JavaScript
- **Анимации**: Framer Motion, GSAP
- **Скролл**: `@studio-freight/react-lenis`
- **Роутинг**: `react-router` 7.x

## 🏗 Архитектура
- Компонентная архитектура с акцентом на анимации и плавный скролл.
- Роутинг с анимированными переходами через `AnimatePresence`.

Пример — App с Lenis и AnimatePresence:
```jsx
// src/App.jsx (фрагмент)
<ReactLenis root>
  <div className="app">
    <AnimatePresence mode="wait" initial={false}>
      <Routes location={location} key={location.pathname}>
        <Route index element={<Home />} />
        <Route path="/work" element={<Work />} />
      </Routes>
    </AnimatePresence>
  </div>
</ReactLenis>
```

## 🎨 UI/UX и стилизация
- Кастомный UI с эффектами курсора и видео фона, плавные переходы.
- Доступность: нужно проверить контрасты/фокус при интенсивных эффектах.

## ✅ Качество кода
- ESLint конфиг присутствует.
- Тесты: отсутствуют.
- Документация: README, минимальные комментарии.

## 🔧 Ключевые компоненты
- `Transition` — анимированные переходы маршрутов
- `HeroGradient` — декоративная графика
- `NavBar`, `BackButton` — навигация

## 📋 Выводы и рекомендации
- Сильные стороны: плавность интерфейса, модульность компонентов.
- Улучшения: добавить тесты и централизованный обработчик ошибок, рассмотреть TS.
- Уровень: middle.