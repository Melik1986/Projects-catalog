/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "images.pexels.com",
            "images.unsplash.com",
            "cdn.imagin.studio",
        ],
    },
}

module.exports = nextConfig
